# Design notes — running log

## Design token proposal (v1) — for principal review before any CSS is written

Brief §5 explicitly warns against a generic "modern minimal tech startup" look and points at a richer vocabulary: instrumentation, signal plots, phase portraits, oscilloscope traces, engineering drawing conventions. This proposal leans into that directly, and ties the palette to something the principal's own work already produces (the blue/orange "measured vs. identified" contrast that shows up in the Agentic-SysID demo and CorBin-FL comparison plots), rather than picking colors arbitrarily.

### Palette (light mode primary, per brief §5)

| Token | Hex | Use |
|---|---|---|
| `paper` | `#FAF7F0` | Page background — warm off-white, like drafting paper, not clinical white |
| `ink` | `#1C1B18` | Primary text — near-black with warmth, not pure `#000` |
| `signal` | `#1857A4` | Primary accent, links, "measured/actual" trace color |
| `trace` | `#C4622D` | Secondary accent, "identified/model" trace color, used sparingly for contrast pairs (e.g. actual-vs-identified, theory-vs-practice) |
| `grid` | `#DAD4C4` | Hairlines, dividers, subtle graph-paper texture in whitespace |
| `ok` | `#3E6B4F` | Small utility green — reserved for things like "validated" / result badges, used rarely |

Dark mode (optional per brief): inverted on a graphite `#16181B` background, `signal`/`trace` brightened ~10% for contrast, `grid` dropped to near-invisible.

### Typography

- **Display** (headlines, theme labels, the hero line): **Fraunces**, variable font, used only at large sizes with its "soft" optical axis dialed toward literary-but-precise rather than fully decorative. Not Inter, not a generic sans.
- **Body**: **Source Sans 3** — plain, legible, utilitarian; carries the actual reading weight of the site.
- **Mono** (equations, code snippets, dataset names, axis labels, the `[R1]`-style tags): **IBM Plex Mono** — genuinely technical, not decorative-monospace.

All three are open-source, self-hostable (no external font CDN calls needed at runtime, keeps the "no API keys / no bloat" constraint clean).

### Layout concept

- Site is organized by the four research themes, never by audience (per brief §0 — no "For Researchers / For Engineers" split).
- Each theme section is framed like an instrument-panel entry: a thin rule + small mono "channel label" (e.g. `CH.1 — INFORMATION THEORY`) above the theme heading, echoing an oscilloscope's channel indicator without literally drawing an oscilloscope.
- A very faint dot-grid (graph-paper reference, `grid` token at ~4% opacity) sits in generous whitespace areas only — never behind text, never busy enough to read as decoration first.
- Two-column proof format everywhere a theme or system is shown: framing prose on the left, one real figure/plot on the right. No card-grid-of-icons pattern (that's the generic-template tell the brief warns against).

### The one signature element (brief §5: "spend boldness in one place")

The hero. Per brief §5's explicit suggestion: open with the Agentic-SysID demo — the real pendulum plant and the identified model swinging in lockstep, styled as a two-channel readout using exactly the `signal` (actual) / `trace` (identified) pair from the palette above, so the accent colors on this one bold element *are* the content, not decoration layered on top. Muted, autoplaying, pauseable, `prefers-reduced-motion` swaps it for a static frame. Everything else on the site stays quiet: no other motion, no gradients, no icon grids.

### What I'm deliberately not doing

- No particle backgrounds, no auto-typing text, no scroll-jacking (explicitly banned, brief §5).
- No stock photography or abstract "AI" imagery — every image is a real result or a real figure.
- Not using al-folio's default Inter/Roboto stack or its card-heavy publication/project list styling — heavily de-templating per brief §5's requirement that the site "must not be recognizable as al-folio."

---

## Decisions log

- 2026-07-23: `Hojat-Salehi.github.io` did not exist yet on GitHub; principal creating it now via github.com/new (empty, no README/license) since repo creation needs the web UI or an API token, not just the SSH deploy key already set up for push/pull.
- 2026-07-23: No local Ruby/Jekyll/Node available in this build environment (confirmed — no interpreter, no environment-modules entry, conda present but non-functional). Chosen approach: push incrementally to the live repo and use GitHub Pages' own remote build as validation, since it's the same engine that renders the real site. Principal confirmed this tradeoff is acceptable (repo not yet announced anywhere).
- Work-authorization line, confirmed final wording: "Currently authorized to work in the US through OPT with STEM extension, no employer sponsorship required."
- No Scholar citation counts will be displayed on the Publications page (principal's call, brief §6.1 required asking before displaying them).
- Custom domain (hojatsalehi.com) not purchased yet — building against the default `hojat-salehi.github.io` URL for now; principal will reconsider once the rest of the site is finalized (confirmed 2026-07-24).
- 2026-07-24: work-authorization line revised to "Currently authorized to work in the US through OPT (eligible for STEM extension), no employer sponsorship required." — principal's preferred phrasing over the original draft.
- 2026-07-24: **canonical name vs. display name split**, per brief §8 — `site.first_name`/`site.last_name` stay "Hojat Allah"/"Salehi" (canonical, matches publications; used in footer copyright, `<meta name="author">`, and the JSON-LD Person schema so search engines/citation tools match the right identity). Added `site.display_name: "Hojat Salehi"`, used only in the three purely visual/human-facing spots: the H1 on Home, the nav-bar brand on other pages, and the browser tab title / social-share title. Principal wanted "Allah" dropped from the visible name but not from the parts that need to match his publication record.
- 2026-07-24: `_pages/about.md`'s `title: blank` (a value with special meaning elsewhere in the theme — see below) was leaking into the nav bar as the literal, visible string "blank" for the Home nav item. Changed to `title: Home`. Confirmed safe: the only other place that checks for the literal string `"blank"` is `metadata.html`'s browser-tab-title logic, which is guarded by `page.url != "/"` too, so Home's own tab title is unaffected.
- 2026-07-24: `max_width` raised from 800px to 1040px for a roomier desktop/laptop layout. This is only an upper bound on the content column (`_sass/_layout.scss`), so mobile is unaffected either way — a phone viewport is always narrower than either value.
- 2026-07-24: removed the RSS icon from the social row (`rss_icon: false`) — it's the `fa-rss-square` icon, which does look like a WiFi signal, and it linked to a feed with zero posts. Re-enable once the Notes blog (Phase 4) has real content.
- 2026-07-24: **photo added** (`PHOTO.jpg`, supplied by principal) and processed into three assets with Pillow (no ImageMagick available in this environment, but Pillow 8.3.2 is):
  - `assets/img/profile.jpg` — head/shoulders crop, source crop box `(0.08w, 0, 0.92w, 0.70h)`, resized to 900px wide.
  - `assets/img/favicon.png` — tight square face crop, source crop box `(0.30w, 0.02h, 0.78w, 0.02h+0.48w)`, resized to 192×192.
  - `assets/img/og-image.png` — 1200×630 generated card: the favicon crop masked into a circle (380px), name in NimbusSans-Bold 74pt, tagline in NimbusSans-Regular 34pt, URL in DejaVu Sans Mono, on the NOTES.md paper/ink/signal palette. Fonts used from system paths under `/usr/share/fonts/urw-base35/` and `/usr/share/fonts/dejavu/`.
  - **To regenerate with a new photo:** replace the source file and re-run the same crop-box ratios (they're relative to the source image's own width/height, so they should reframe reasonably on a similarly-composed portrait photo) — or just ask me to redo it.
- 2026-07-24: ORCID set (`0000-0002-4186-9349`). dblp and Semantic Scholar profiles will **not** be created — principal's call, Google Scholar already covers citation discovery and it's not worth maintaining extra profiles.
- 2026-07-24: **Phase 2 started — Systems page, one system at a time.** Replaced `_pages/projects.md` (al-folio's default card-grid page, driven by the unused `_projects` collection) with `_pages/systems.md`, driven by `_data/systems.yml` and a custom `_includes/system_card.html` partial rendering the brief's required Problem → Approach → Result → Artifact structure — never a feature list, never al-folio's icon-grid pattern (brief §5/§6.2). Removed the now-unused `_projects` collection entry from `_config.yml` and the unused `_includes/projects.html` / `projects_horizontal.html` templates. Added `_sass/_systems.scss` for the card layout, using the theme's existing `--global-*` CSS custom properties so it already works in light/dark — full bespoke visual pass still lands with Task #7.
  - `nav: false` on `/systems/` until all four systems have real cards — will flip to `true` once the Systems page as a whole is ready, consistent with the brief's "every card has a figure and a real number" gate for Phase 2.
- 2026-07-24: **CorBin-FL Systems card — fully sourced, no principal input needed.** Figure 1 (four-panel method schematic: Initialization / Secure Pairwise Link / Model Update and Share Seeds / Aggregation) was rasterized from `CorbinFL_paper/CorbinFL_Method (1).pdf` via Ghostscript (`gs -sDEVICE=pngalpha -r300`, no `pdftoppm`/ImageMagick available in this environment), trimmed of whitespace and resized to 1600px wide with Pillow. The PCA error-concentration figure came straight from `repos/CorBin-FL/pca_all_weights.png` (flattened alpha to white, resized to 1400px, optimized — 521KB → 420KB). The ten-baseline comparison table and the CIFAR-10/FEMNIST accuracy numbers are both transcribed directly from `repos/CorBin-FL/README.md`'s own tables — nothing computed or estimated.
- 2026-07-24: **Agentic-SysID Systems card — partially sourced, one `[TK]` filed.** The architecture flowchart came from `repos/Agentic-SysID/demo/FlowChart.png`. The parameter-accuracy numbers (K_u/K_d/K_s within 1–3% of true values) and the 10.05° RMSE came from reading the last frame of `demo/demo.gif` with Pillow (`im.seek(im.n_frames - 1)`) — those are real numbers baked into the animation by an actual pipeline run, not invented. What I could *not* get: experiments-consumed-per-run and fidelity-ladder-descent frequency, both of which the brief explicitly asks for — the experiment database that would answer this (`data/`) is gitignored and absent from this clone. Filed as Q-006 in `QUESTIONS.md` rather than guessing.
- 2026-07-24: **game-analyst-multiagent Systems card — architecture only, no track record.** No server-side Mermaid renderer is configured on this site (removed with the `jekyll-diagrams`/`mermaid.cli` cleanup during Phase 1 CI fixes), so the repo README's Mermaid flowchart was re-rendered as a static diagram with Graphviz (`dot`, available locally — no `mmdc`/npm/node in this environment) — same nodes, same edges, same labels, just a different renderer. The brief's required "measured prediction accuracy over N games" and "linked sample HTML report" are **not obtainable at all** from this repo clone: `*.db`, `reports/`, and `memories_db/` are all gitignored and empty — there is no run history to extract, period. Filed as Q-007. This is flagged as the honest current weak point of this system, not smoothed over.
- 2026-07-24: **DDIM_Time_Series Systems card — fully sourced, no principal input needed.** Both figures and the MSE ≈0.21 result number came straight from `repos/DDIM_Time_Series/README.md` and its `test_results/` folder (already exactly the "three-channel RGB conditioning input beside generated-vs-actual output" the brief asked for — no rendering or extraction needed, just cropping whitespace and resizing). Used `test_results/comparisons/sample_0.png` (original vs. generated) plus `test_results/intermediate_steps/sample_0_steps.png` (the 12-step denoising trajectory) instead of `linkedin_visuals/linkedin_visualization.png`, which has a genuine rendering bug (a clipped/overlapping text label in its top-left panel) — not worth using on the actual site even though it's a nicer composite otherwise.
- 2026-07-24: **All four Systems cards done — `/systems/` now in the nav.** Per the brief's own acceptance line for this page ("every card has a figure and at least one sourced number **or a tracked `[TK]`**"), the Game Analyst card's `[TK]` counts as meeting the gate, not blocking it — Phase 2 Systems half is done. Research page (four theme framings) is next; nav stays scoped per-page so Systems didn't need to wait for it.
- 2026-07-24: **Research page added — Phase 2 complete.** Four themes (`_pages/research.md`), each with plain-English framing (`DRAFT-COPY`) linking straight through to the matching `/systems/#anchor` card and the real papers already in `_bibliography/papers.bib` — no query-DSL dependency on `jekyll-scholar`'s `{% bibliography %}` tag here (untestable without a live Jekyll build in this environment), just hand-written links to each paper's own arXiv/publisher URL, reusing exactly the titles/venues/links already vetted for the Publications page. One addition beyond the brief's original four-theme paper list: `DecoSearch` (arXiv 2606.17821) slotted into Agentic Systems — it postdates the brief and fits that theme better than any other. The DiGress graph-explainability work (theme 4) has no repo of its own, just a CV bullet, so it's mentioned in prose with the same external DiGress/ProxyExplainer links already used in `CV.tex`, not given a fake Systems card.
- 2026-07-24: **Nav reordered** to match the reading flow the brief's own Home-page "start here" router describes (research reader → Publications, engineering reader → Systems, CV for the control-engineering decade): Home, Research, Systems, Publications, CV (`nav_order` 1–5). Previously Publications was 2 and CV was 3 with nothing after.
- 2026-07-24: **Bespoke Sass design system implemented (Task #7), NOTES.md proposal made real.** Self-hosted Fraunces (display), Source Sans 3 (body), IBM Plex Mono (mono/labels) — downloaded as variable-font WOFF2 from the same source repos Google Fonts itself builds from (`google/fonts` on GitHub, all three OFL-licensed, license texts kept in `assets/fonts/LICENSES/`), converted TTF→WOFF2 with `fontTools` (no `woff2_compress`/npm available, so `pip install --user fontTools brotli` + `TTFont.flavor = "woff2"` in Python instead) — 2.1MB of TTF became 812KB of WOFF2. Removed the `fonts.googleapis.com/css?family=Roboto...` CDN link from `head.html` entirely (nothing on the site referenced Material Icons or Roboto Slab, confirmed by grep before removing). Palette rewired in `_sass/_variables.scss`/`_themes.scss` to the paper/ink/signal/trace/grid/ok tokens, both light and dark mode, as real CSS custom properties (`--global-signal-color`, `--global-trace-color`, etc.) rather than one-off hex values, so every component built on top of them (Systems tables, Research venue badges, the hero) stays theme-aware automatically. One deliberate bonus: link **hover** state now genuinely shifts signal-blue → trace-orange site-wide (`--global-hover-color: $trace-color`) — reinforces the measured/identified color language everywhere, not just in the hero, at zero extra cost since it reuses al-folio's existing hover-color hook.
  - Instrument-panel channel-label motif (`CH.1 — INFORMATION THEORY` / `SYS.01`) added to Research theme sections and Systems cards; a very faint (7%/5% opacity) dot-grid texture added to `body` background site-wide.
  - Two-column proof format (prose left, real figure in a sticky right rail) applied to both Research theme sections and Systems cards — Systems cards keep their brief-mandated Problem→Approach→Result→Artifact structure, just laid out in the left column instead of stacked above the figures.
  - **Hero** (`_includes/hero.html`, `_sass/_hero.scss`, `assets/js/hero.js`): the real Agentic-SysID `demo.mp4` (232KB, copied from the repo) as a muted/looped/autoplaying two-channel readout — "Actual plant" in signal blue, "Identified model" in trace orange — with a real pause/play button (not decorative) and a poster frame (the same `agentic-sysid-demo-frame.png` used on the Systems card). `prefers-reduced-motion` is handled in JS: the video simply never calls `.play()`, so it sits on the poster frame — a true static-frame fallback, not just a hidden autoplay — and the toggle button is hidden via CSS media query since there's nothing to toggle. Placed on Home between the header (name/photo/currently-line, unchanged) and the existing intro prose, so the personal-identity block the principal asked for stays first, and the bold signature moment follows immediately after.
- 2026-07-24: **Hero re-sized and re-positioned twice on principal's feedback.** First pass had it at 560px max-width, directly after the `<header>` and before the profile photo/bio — principal reported it "so large" and placed above his description. Shrunk to 340px, and moved from the layout (`_layouts/about.html`, between header and `<article>`) into the markdown content itself (`_pages/about.md`, via `{% include hero.html %}` between the bio paragraph and the "four themes" paragraph) — so photo, currently-line, and bio all render first, and the hero now reinforces the intro instead of preceding it.
- 2026-07-24: **Q-006/Q-007 resolved as "won't chase further."** Principal's call: Agentic-SysID and Game Analyst are research prototypes he hasn't run in real time / doesn't test against real games right now — they're meant to be descriptive, run-it-yourself systems, not systems with a live track record. Trimmed both cards' `result:` text in `_data/systems.yml` — dropped the internal planning language ("the biggest gap on this entire site," "by interview season") that had leaked into public-facing copy, replaced with a short, neutral `[TK]` note. Revisit only if he starts actually running either system for real.
- 2026-07-24: **Phase 3, Demo 1 shipped — CorBin-FL privacy/accuracy explorer (`/demos/`).** `results.log` alone turned out to be missing the star method: no `CorbinFL` × `CIFAR10` sweep exists in it at all, and it covers only CIFAR-10/MNIST (no FEMNIST/Shakespeare/Sent140/Reddit), only 5 of ~10 methods (missing Laplace, LDPFL, FedAvg, SignSGD, Gaussian LDP/CDP, AugCorBin-FL entirely). Combined it with the README's own results tables (which happen to have exactly the methods `results.log` lacks, for CIFAR-10 and FEMNIST at ε∈{1,5}) to get real, sourced coverage across three datasets instead of fabricating the rest — every data point in `assets/data/corbin_sweep.json` carries a `source` field (`results.log` or `README.md`) and a `note` field where seed completion was partial (e.g. PBM-PLDP 2-bit on CIFAR-10 ran only 1–2 of 5 seeds for some ε). Filed Q-008 asking if a fuller `results.log` can be regenerated from his local `results/` directory (gitignored, not in this clone) — not blocking, demo ships with what's real.
  - Built the chart as hand-rolled SVG + vanilla JS (`assets/js/corbin-explorer.js`, `_sass/_demos.scss`) rather than vendoring a chart library — the dataset is small (≤9 series, ≤9 points each) so this was fully tractable, keeps page weight minimal, and lets CorBin-FL render as the visual "hero" (thick signal-blue line) against muted trace-orange baselines and dashed grey non-private reference lines, instead of a generic chart-library look. Log-scaled ε axis, hover tooltips with exact accuracy/std/bits-per-coordinate/privacy-class/source, legend toggles per method, dataset tabs (CIFAR-10/MNIST/FEMNIST).
  - Added `_pages/demos.md` (`nav_order: 4`, between Systems and Publications) and bumped Publications/CV to 5/6.
- 2026-07-24: **Demo 1 finalized — rebuilt from the paper itself, not just the repo.** Principal pointed at `CorbinFL_paper/corbinfl.tex`'s own results tables. These turned out to be a strict superset of everything I'd extracted before: `tab:MNIST` gives full 4-point ε-sweeps for both CIFAR-10 and MNIST across 6 methods each — including the CorBin-FL × CIFAR-10 points that were the original gap (`results.log` never had that combination at all) — plus Laplace/LDPFL numbers for MNIST that weren't anywhere in the repo clone. `tab:all` added one supplementary Gaussian-mechanism point and one Augmented CorBin-FL point (CIFAR-10, ε=5, but under a *different* 300-round protocol than the main 100-round table — kept as a separate `note` on those two points rather than silently mixed in as if directly comparable). Rebuilt `assets/data/corbin_sweep.json` entirely from these tables (`generated_from` now cites the paper directly).
  - Shakespeare and Sentiment140 don't fit the ε-sweep chart at all — the paper evaluates them as accuracy-vs-communication-round convergence curves at one or two fixed ε values, a fundamentally different plot shape. Rather than force them into the interactive chart (or worse, eyeball-extract fake sweep points from a line plot), rasterized the paper's own published figures (`Shakespeare_epsilon_1_test_accuracy_comparison.png`, the combined `Sent140_test_accuracy_comparison.png`) into `assets/img/demos/` and added them as a labeled second section on `/demos/`, explicit about being a different chart type.
  - Reddit: confirmed by grepping the full paper — not evaluated anywhere, only listed as a supported dataset in the code. Nothing to extract, nothing fabricated.
  - Fixed a real (if minor) bug while doing this: the SVG marker-shape cycling counter was incrementing for non-private flat-reference series too (CorQuant/FedAvg), even though those don't use a marker shape — meant shapes silently drifted out of sync between plotted series and the legend once CIFAR-10 grew past 5 baseline methods. Excluded flat series from the shape counter.
  - Q-008 downgraded from "open" to "resolved, with one confirmed non-gap" (Reddit) in `QUESTIONS.md`.
- 2026-07-24: **Demo 2 shipped — in-browser MNIST saliency, no ONNX Runtime Web.** Brief §6.3 names ONNX Runtime Web explicitly, but before writing any code I downloaded the actual `onnxruntime-web@1.27.0` npm tarball to check real file sizes: the smallest usable WASM backend (`ort-wasm-simd-threaded.wasm`) is **13.5MB**, to run a model (`Utils.SimpleCNN`, 2 conv layers + 1 linear layer) with about 20,000 parameters — 6-7x over the brief's own "page weight under 2MB" demo budget. Grad-CAM also needs a backward pass, which ONNX Runtime Web's inference-only WASM build can't do at all, so using it would only have covered the forward pass and I'd still need custom backward math regardless. Flagged this conflict to the principal directly (via `AskUserQuestion`, not silently) with the measured numbers; he chose pure JS over keeping ONNX Runtime Web as a resume line-item.
  - Implementation: extracted both `.pth` checkpoints' raw weights (`conv1.weight/bias`, `conv2.weight/bias`, `fc.weight/bias`) to flat `Float32Array` binaries via `torch.load` (torch 1.9.1 is available in this environment) — 82KB per model, 164KB total for both, versus 13.5MB+ for the WASM engine alone. `assets/js/mnist-saliency.js` implements conv2d/relu/maxpool(with argmax tracking)/fc/softmax from scratch, plus Grad-CAM (`target_layers=[model.conv2]`, matching the repo's own `Explainability.ipynb` exactly) computed analytically: since `fc` is linear, `d(logit_c)/d(pool2_flat) = fc.weight[c,:]` exactly, then routed backward through maxpool (via the recorded per-window argmax) and through relu (zeroed where the raw conv2 output was ≤0) to get the true gradient at the Grad-CAM target layer — no autograd engine needed.
  - **Verified against PyTorch itself before porting to JS**, not just spot-checked: wrote a NumPy reimplementation of the forward pass and diffed it against `model(x)` on a random input (max abs diff ~2e-6, float32 noise floor) — then wrote the analytical Grad-CAM gradient and diffed it against PyTorch's real `.backward()` output via a forward hook with `retain_grad()` on the conv2 output (max abs diff **exactly 0.0**). The JS is a direct line-by-line port of this already-verified NumPy code.
  - Heatmap colormap uses the site's own signal→trace accent pair (low importance → high importance) instead of a generic rainbow/jet colormap, consistent with the rest of the site.
  - The "framed MNIST" distribution-shift story (brief's explicit ask: "confirm with the principal before asserting what the contrast demonstrates") is already unambiguous and well-documented in the repo's own README — no interpretation needed, just reproduced faithfully. Copy was still hedged on one point: the README's exact "99%→10%" collapse was measured on real MNIST test images with the frame added programmatically, not on mouse-drawn browser input, and this environment has no way to test a live canvas in an actual browser — so the demo copy invites exploring the heatmap shift rather than promising an exact numeric reproduction.
  - **Found and fixed a real hygiene issue in `AI_Explainability`** while reading its README for this: the clone instructions still pointed at `github.com/AIDeveloperSalehi/...` — the brief's Definition of Done explicitly bans this exact old username — even though the actual git remote is correctly `Hojat-Salehi`. Fixed and pushed directly to that repo.
- 2026-07-24: **Phase 4 scaffolded — blog structure + four post outlines, none published yet.** Demo 3 stays blocked (no real Game Analyst transcript to replay), so this was the next unblocked step. Found `blog/index.html` still had al-folio's original `jekyll-paginate-v2` pagination front matter (`{% for post in paginator.posts %}`, a global `pagination: {enabled: true}` block in `_config.yml`) left over from when that plugin was trimmed out during Phase 1 CI fixes — `paginator` would just silently resolve to nil rather than error, but it's dead config referencing a gem that isn't installed. Rewrote to a plain `site.posts` loop (Jekyll core, no plugin), deleted the now-unused `_includes/pagination.html`, removed the dead `pagination:` block from `_config.yml`.
  - Added four posts to `_posts/`, each `published: false` so they're scaffolded in the real location/format (flip the flag or delete the line when written) without going live or needing `blog_nav_title` set — real outlines (structure, what to lead with, what figures/data already exist to pull from, what's still honestly open per `QUESTIONS.md`), not drafted prose, per the brief's "principal writes these." Topics are the brief's own suggested four, mapped to what's actually real on the site now: correlated-noise/CorBin-FL (links the demo + PCA figure), non-interactive source simulation/entanglement (flagged as having no figure of its own yet, unlike the other three), Agentic-SysID system-identification (links the demo video/frame), and GNN explanation distribution-shift (flagged as having no repo/figure available in this environment at all, unlike the other three which do).
